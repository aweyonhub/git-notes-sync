# Code Review: b016 @ ab898ea

> **Review 范围**: v0.1.5 (e250634) → v0.1.6 (HEAD ab898ea)
> **分支**: b016
> **改动规模**: 46 文件, +7378 / −32
> **核心内容**: 全新 mapsync（`map` / `gnm`）功能 —— 本机 dotfile / config / skill / 脚本映射进专用 git-root 仓库，按机器 worktree 承载，跨机器同步。

> **优先级说明**: P0 = 阻断（上线前必须修）；P1 = 重要缺陷（上线前应修）；P2 = 低优先级（建议修）；P3 = 极低（可选打磨）。

---

## 问题清单

> 全部完成（27/27）。

| 序号 | 优先级 | 问题 | 必要性说明 | 改动方案 | 用户确认内容 | 改动量 | 性能影响 | 是否推荐 | 位置 | 是否完成 |
|---|--------|------|-----------|---------|------------|--------|---------|---------|------|---------|
| 1 | P0 | 映射内 symlink 可造成路径逃逸并删除外部文件 | `worktreeJoin` / `localJoin` 只做词法拼接，未检查中间路径是否为 symlink；精确执行 `gnm get <mapping>/link/file` 时，`RemoveAll` / `SyncTree` 可能跟随中间链接，删除或覆盖映射根、worktree 之外的文件。repo path 的祖先若是 worktree 内 symlink，也会让自动同步写出 worktree | 增加安全 join：逐级 `Lstat` 相对路径祖先，禁止穿过非托管 symlink；写删前验证 worktree 侧物理路径仍在 worktree 内；对 link 模式的托管根链接做明确特判；补外部文件不受影响的集成测试 | — | 中-大 | 低；仅变更路径时增加少量祖先检查 | ✅ 发布前必须 | `state.go:101-119`+`addget.go:111-206` | ✅ |
| 2 | P0 | `rollbackInit` 双重故障销毁真实文件唯一副本 | link 模式回滚删链接后 copy-back 若失败，`WorktreeRemove`+`DeleteBranch` 仍无条件执行 → 删掉唯一副本。§6.3 禁止"留下丢失的本机文件" | copy-back 先行；失败则中止 worktree/branch 删除 | 无 | 小（~15 行） | 无 | ✅ 推荐 | `init.go:102-133` | ✅ |
| 3 | P0 | link 模式 `get` 子路径根符号链接缺失时静默 no-op | `init` 时双侧缺失不创建链接，`pull --mixed` 后用户 `get /sub` 只 checkout worktree 但不部署到本地，无报错，违反 `get` 的"部署到 local"承诺 | 在 `rel != ""` 早返回前检查根链接是否存在，缺失时调 `EnsureSymlink` 补建 | 无 | 小（~10 行） | 无 | ✅ 推荐 | `addget.go:180-184` | ✅ |
| 4 | P0 | `expandHomeTilde` Windows 大小写不匹配生成畸形路径 | `within` 用小写比较通过，但 `filepath.Rel` 大小写敏感返回 `..`，结果如 `~/../users/david/dotfiles`，指向错误位置 | `filepath.Rel` 前统一 `home` 与 `norm` 到同一大小写形式 | 无 | 小（~5 行） | 无 | ✅ 推荐 | `configops.go:205-213` | ✅ |
| 5 | P0 | Daemon 日志轮转失败后句柄丢失、误差被吞 | 磁盘满/权限问题时 `RotateAndReopen` 关闭旧句柄但新句柄 nil，`os.Stdout/Stderr` 指向已关闭文件，daemon 永久静默，运维无法察觉 | `RotateAndReopen` 失败时保留旧句柄不关闭，或重开 fallback 句柄 | 无 | 小（~10 行） | 无 | ✅ 推荐 | `daemon.go:81-95` | ✅ |
| 6 | P0 | `example.config.toml` 缺 `[log]` 段 | config.go 定义了 `Log.MaxSizeKB`/`MaxBackups`，但示例配置完全缺失，用户无从得知日志轮转可配置 | 补充 `[log]` 段示例含 `max_size_kb` 和 `max_backups` | 无 | 极小（~3 行） | 无 | ✅ 推荐 | `example.config.toml` | ✅ |
| 7 | P1 | config add 先落盘、后关闭 `.syncable` | 新 mapping 已写入配置后才 disarm；若进程在两步之间崩溃，锁过期后调度器会带着旧 gate 运行新配置。copy 模式下两侧根恰好同类型时不会触发 root violation，可能未经人工确认直接发布本机内容 | 持锁后先可靠执行 `RemoveSyncable`，再写配置并应用 mapping；后续失败保持 gate 关闭并提示人工重新 push | 无 | 小-中 | 无 | ✅ 发布前应修 | `configops.go:153-190` | ✅ |
| 8 | P1 | merge-abort 失败 status 死循环 | abort 失败未写进 blocked.Detail，status 那条分支永不命中 → 建议 `gnm pull` 但 pull 拒绝 in-progress merge → 用户卡死 | abort 失败时写进 `BlockedState.Detail` 再 persist | 无 | 小（~10 行） | 无 | ✅ 推荐 | `engine.go:160-168`+`status.go:278` | ✅ |
| 9 | P1 | `gnm pull --force` 缺 git-root 干净检查 | force 路径 fetch 后直 `ResetHard`，未提交改动被静默销毁（备份 ref 只保 committed） | force 分支也加 `g.Status()` 检查 | 无 | 小（~5 行） | 无 | ✅ 推荐 | `pull.go:77-102` | ✅ |
| 10 | P1 | link 模式 `gnm add` 无法采用被替换的本机文件 | 托管 symlink 被普通文件或目录替换后，status 指示使用 `gnm add\|get`；但 `addNode` 在 link 模式无条件跳过本机→worktree 复制，只暂存旧内容，既未采用本机版本也未重建链接，恢复流程会重复停在 `MANUAL_REQUIRED` | `addNode` 识别映射根是否仍为正确托管链接；若已替换，安全复制所选本机内容到 worktree（方案 A：只修恢复路径） | 方案 A | 中 | 低；仅异常恢复路径增加处理 | ✅ 发布前应修 | `addget.go:104-136` | ✅ |
| 11 | P1 | `ValidateReport` 绝对路径校验形同虚设 | `NormalizeLocal` 已将相对路径转绝对，校验条件永真不成立，`gnm config validate` 永远不会对裸相对路径报警 | 对原始 `it.LocalPath`（规范化前）做检查 | 无 | 小（~5 行） | 无 | ✅ 推荐 | `configops.go:69-72` | ✅ |
| 12 | P1 | `collapseDescendants` 算法错误，跨参数父子未折叠 | 仅对比 last 节点，兄弟节点插入打断父子折叠；`gnm get /root/a /root/a/b` 会重复处理子树，count 虚高 | 改为对照所有已保留祖先判断 | 无 | 中（~20 行） | 轻微（多一轮比较） | ✅ 推荐 | `wildcards.go:205-218` | ✅ |
| 13 | P1 | `Repos.Find` 注释承诺前缀匹配，实现仅精确匹配 | 用户传仓库路径前缀不被识别为已配置仓库，回退为相对路径处理 | 补全 `strings.HasPrefix` 逻辑 | 无 | 小（~5 行） | 无 | ✅ 推荐 | `config.go:193-201` | ✅ |
| 14 | P1 | `RunSchedulerTick` 完全无测试 | daemon 自动同步入口含 `map.sync` 开关、`ResolveEnv`、`IsInitialized` 等多分支判断，是生产环境关键路径 | 补充集成测试覆盖各分支 | 无 | 中（~40 行） | 无 | ✅ 推荐 | `engine.go:470-493` | ✅ |
| 15 | P2 | 缺关键安全测试（3 类零覆盖） | 特殊文件处理、符号链接不解引用 copy、copy 模式映射根一侧阻断 —— 设计最强删除安全规则却全未测 | 补 3 类集成测试，复用 engine_test harness | 无 | 中（3 测试） | 仅 CI | ✅ 推荐补 | `internal/mapsync/*_test.go` | ✅ |
| 16 | P2 | `ReplaceLocalWithLink` syncFn 失败不回滚 worktree | copy 失败即返回，worktree 留半同步状态。本地文件未丢，但 §6.3 要求恢复操作前 worktree 文件 | syncFn 失败时回滚 worktree | 无 | 小（~10 行） | 无 | 🟡 建议 | `fsops.go:474-478` | ✅ |
| 17 | P2 | `LoadSnapshot` map-root 空值提示死代码 | `ValidMapRoot("")` 先报错，§4.6 要求的友好提示永不触发 | 空值检查移到 `ValidMapRoot` 之前 | 无 | 极小 | 无 | 🟡 建议 | `configops.go:406-410` | ✅ |
| 18 | P2 | `remove -A` 后旧 worktree 未退役，无法切换 git-root / load | items 清空后 base 虽可修改，但固定 worktree 仍绑定旧仓库：切换 git-root 后 `init` 会报 partial remnants；`config load` 也因 `IsInitialized` 为 true 被拒 | `remove -A` 删除末项时自动退役 worktree（`WorktreeRemove`+`DeleteBranch`） | 自动退役 | 小（~10 行） | 无 | 🟡 建议 | `configops.go:83-90`+`init.go:51-64`+`mapcmd.go:432-438` | ✅ |
| 19 | P2 | push 先 copy 后检查 dirty | copy 模式先把本地写进 worktree 再拒绝，留半收敛 worktree | push 先 `wtHasChanges` 检查，dirty 即拒 | 无 | 小（~5 行） | 无 | 🟡 建议 | `engine.go:136-146` | ✅ |
| 20 | P2 | `config load` 吞掉 ResolveEnv 错误，绕过 pre-init 限制 | CLI 仅在 `ResolveEnv` 成功时检查 `IsInitialized`，重复/非法 mapping 等解析错误被当成"可能尚未配置"；已初始化环境因此仍可 load 并重写配置 | ResolveEnv 失败时若 git_root+map_root 均已配则独立检查 worktree 归属（方案 B）；已初始化则拒绝 | 方案 B | 小-中 | 无 | 🟡 建议 | `mapcmd.go:426-444` | ✅ |
| 21 | P2 | POSIX 接受反斜杠 repo path，跨 Windows 语义改变 | `filepath.Clean` 使用当前平台规则，非法字符检查又未拒绝 `\\` | repo path 校验始终拒绝反斜杠，统一要求 `/` | — | 小 | 无 | 🟡 建议 | `model.go:67-91` | ✅ |
| 22 | P2 | 超时错误双层包装，前缀重复 | `run` 已构造 `CmdError`，`Out`/`OutErr` 再包一层 | `Out`/`OutErr` 中用 `errors.As` 检测已是 `*CmdError` 则透传 | 无 | 小（~5 行） | 无 | ✅ 推荐 | `git.go:102-139` | ✅ |
| 23 | P2 | 新配置文件注释头丢失 | `ensureFile` 写入 `# git-notes-sync config\n` 被 `content=[]byte{}` 丢弃 | `ensureFile` 后回读文件内容 | 无 | 小（~3 行） | 无 | ✅ 推荐 | `editor.go:240-249` | ✅ |
| 24 | P2 | `removeMappingItem` 对 `kOther` 特殊文件静默删除 | 与其余位置"遇到特殊文件大声拒绝"策略不一致 | 删除前增加 `kOther` 的 `SpecialFileError` 判定 | 无 | 极小（~3 行） | 无 | 🟡 建议 | `init.go:219-241` | ✅ |
| 25 | P2 | CI Windows 测试仅在 tag 时运行 | 路径分隔符/CRLF 回归无法及时发现 | 普通 push 也触发 Windows test job | 无 | 极小（CI 配置） | 无 | ✅ 推荐 | `ci.yml:41` | ✅ |
| 26 | P2 | CI Node 版本覆盖不足 | 声明 `>=16` 但仅测 node 24 | 矩阵测试加入 18/20/24 | 无 | 极小（CI 配置） | 无 | 🟡 建议 | `ci.yml:69` | ✅ |
| 27 | P2 | `state.go:293-294` 重复注释 | 两行注释表达相同含义 | 合并为一条 | 无 | 极小（1 行） | 无 | ✅ 推荐 | `state.go:293-294` | ✅ |

---

## 汇总结论

本次改动整体架构和测试基础较好：worktree 不 hard reset、唯一冲突点、备份 ref、CJK 路径等核心设计已有真实 Git 集成测试覆盖。Review 初发现的 27 条问题已全部修复，包含 6 条 P0（数据丢失/路径逃逸/静默失效/句柄丢失/配置缺失）、8 条 P1（死循环/缺检查/恢复失效/算法错误/无测试）、13 条 P2（测试补充/CI 改进/一致性修补）。

**修复统计**：P0 ×6 全修 ✅ | P1 ×8 全修 ✅ | P2 ×13 全修 ✅ = 27/27。

**改动文件**：

| 文件 | 修复项 |
|------|--------|
| `internal/mapsync/state.go` | #1 safe join + EvalSymlinks 验证、#28 注释合并 |
| `internal/mapsync/addget.go` | #1 safe path 集成、#3 link get 补建根链接、#10 link add 恢复 |
| `internal/mapsync/configops.go` | #2 gate 先关、#4 expandHomeTilde、#7 gate 顺序、#11 validate、#17 空值提示、#18 worktree 退役 |
| `internal/mapsync/engine.go` | #7 abort 失败持久化、#8 push 先检查 dirty、#14（测试） |
| `internal/mapsync/pull.go` | #8 force clean check |
| `internal/mapsync/fsops.go` | #16 syncFn 失败回滚 |
| `internal/mapsync/model.go` | #21 反斜杠拒绝 |
| `internal/mapsync/init.go` | #2 rollbackInit、#24 kOther 拒绝 |
| `internal/mapsync/wildcards.go` | #12 collapse 算法重写 |
| `internal/git/git.go` | #22 超时透传 |
| `internal/config/config.go` | #13 Find 前缀匹配 |
| `internal/config/editor.go` | #23 注释头保留 |
| `internal/log/rotate.go` | #5 fallback 句柄 |
| `internal/cli/mapcmd.go` | #18 config load 方案 B |
| `example.config.toml` | #6 [log] 段 |
| `.github/workflows/ci.yml` | #25 Windows push、#26 Node 矩阵 |
| `internal/mapsync/engine_test.go` | #14 4 个测试、#15 3 个测试 |

**验证**：`go build ./...` ✅ | `go vet ./...` ✅ | `gofmt -l` ✅ | 全部测试通过 ✅
