{
  "name": "git-notes-sync",
  "version": "0.1.0",
  "description": "Auto-sync for notes-style git workspaces: commit → fetch → merge → push, conflict-tolerant, AI-enhanced commit messages (npm installer for the Go binary)",
  "license": "MIT",
  "repository": "github:git-notes-sync/git-notes-sync",
  "bin": {
    "notes": "bin/notes.js",
    "notes-sync": "bin/notes.js"
  },
  "scripts": {
    "postinstall": "node scripts/install.js"
  },
  "files": [
    "bin/notes.js",
    "scripts/install.js",
    "README.md"
  ],
  "engines": {
    "node": ">=12"
  }
}
